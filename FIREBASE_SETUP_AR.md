# تشغيل نظام الحسابات في منصة زاد المعرفة

النظام المرفق يستخدم Firebase Authentication للبريد/كلمة المرور وCloud Firestore لبيانات المستخدمين. يجب تشغيل الموقع عبر Live Server أو استضافة HTTP، وليس فتحه كـ file:// مباشرة.

## 1) إنشاء مشروع Firebase
1. افتح Firebase Console.
2. أنشئ مشروعًا جديدًا.
3. أضف Web App للمشروع.
4. انسخ إعدادات `firebaseConfig` إلى ملف `firebase-config.js`.

## 2) تفعيل تسجيل الدخول
من Firebase Console:
Authentication → Sign-in method → Email/Password → Enable.

## 3) إنشاء قاعدة Firestore
من Firestore Database أنشئ قاعدة البيانات.
بعدها انسخ محتوى `firestore.rules` إلى تبويب Rules.

**مهم:** في `firestore.rules` استبدل:
`ضع_إيميل_الأدمن_هنا@example.com`
بالبريد الذي ستستخدمه كحساب أدمن.

ونفس البريد ضعه في:
`ADMIN_EMAIL`
داخل `firebase-config.js`.

## 4) إنشاء حساب الأدمن
من Authentication → Users → Add user، أنشئ مستخدمًا بالبريد المحدد أعلاه.
لا تجعل المستخدم يختار "أدمن" من صفحة التسجيل؛ الأدمن يتم تحديده من البريد الموجود في قواعد Firestore.

## 5) تشغيل المشروع
افتح مجلد المشروع في VS Code ثم شغّل Live Server، وافتح الرابط الذي يبدأ بـ:
`http://127.0.0.1:`
ولا تفتح الملفات عن طريق `file:///`.

## 6) اختبار
- أنشئ حساب طالب → يدخل لوحة الطالب.
- أنشئ حساب معلم → حالته "في انتظار اعتماد الإدارة".
- ادخل بحساب الأدمن → لوحة الإدارة → اعتماد المعلم.
- بعد الاعتماد أعد فتح لوحة المعلم فتظهر أدواته.
- استخدم "نسيت كلمة المرور" لإرسال رابط الاستعادة.

## ملاحظة أمنية
إعداد Firebase الخاص بتطبيق الويب يمكن أن يكون ظاهرًا في JavaScript؛ الحماية الحقيقية لبيانات Firestore تأتي من Authentication وSecurity Rules. لا تضع Service Account JSON أو أي مفتاح خادم سري داخل ملفات الموقع.
